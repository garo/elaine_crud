<!DOCTYPE html>
<html>
  <head>
    <title>ElaineCrud Demo - Library Management System</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Tailwind CSS from CDN for demo purposes -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Turbo for seamless navigation and inline editing -->
    <script type="module">
      import hotwiredTurbo from 'https://cdn.jsdelivr.net/npm/@hotwired/turbo@8/+esm'
    </script>

    <style>
      /* Additional custom styles if needed */
    </style>
  </head>

  <body class="bg-gray-50">
    <!-- Navigation -->
    <nav class="bg-blue-600 text-white shadow-lg">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <h1 class="text-xl font-bold">📚 Library Management</h1>
            <span class="ml-4 text-sm text-blue-200">ElaineCrud Demo</span>
          </div>
        </div>
      </div>
    </nav>

    <!-- Sub-navigation -->
    <div class="bg-white shadow">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex space-x-8 h-12 items-center text-sm">
          <%= link_to "Libraries", libraries_path, class: "text-gray-700 hover:text-blue-600 font-medium" %>
          <%= link_to "Authors", authors_path, class: "text-gray-700 hover:text-blue-600 font-medium" %>
          <%= link_to "Books", books_path, class: "text-gray-700 hover:text-blue-600 font-medium" %>
          <%= link_to "Members", members_path, class: "text-gray-700 hover:text-blue-600 font-medium" %>
          <%= link_to "Loans", loans_path, class: "text-gray-700 hover:text-blue-600 font-medium" %>
          <%= link_to "Librarians", librarians_path, class: "text-gray-700 hover:text-blue-600 font-medium" %>
        </div>
      </div>
    </div>

    <!-- Flash messages -->
    <% if notice %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded">
          <%= notice %>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded">
          <%= alert %>
        </div>
      </div>
    <% end %>

    <!-- Main content -->
    <main class="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t mt-12">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
        <p class="text-center text-sm text-gray-500">
          ElaineCrud Demo Application - Showcasing CRUD features
        </p>
      </div>
    </footer>
  </body>
</html>
